# Support

Open a GitHub issue with the Node and Stylelint versions, plugin configuration, command, and a minimal stylesheet that reproduces the behavior. Remove credentials and private paths from logs before sharing them.

For summary questions, include whether the command uses the CLI, Node API, an editor, caching, or a watch process. Those choices determine which inputs reach the progress rule and when the process exits.
